var modalName = "#graphicSelection";
$(modalName).remove();
$("body").append("<%= j render partial: "update_graphic" %>");
$(modalName).modal("show");
$(modalName).on('hidden.bs.modal', function (e) {
    $(modalName).remove();
});